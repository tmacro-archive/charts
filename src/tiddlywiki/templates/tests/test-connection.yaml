apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tiddywiki.fullname" . }}-test-connection"
  labels:
{{ include "tiddywiki.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tiddywiki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
